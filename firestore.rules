service cloud.firestore {
  match /databases/{database}/documents {
    match /historico_precificacao/{docId} {
      allow read, write: if request.auth != null && request.auth.token.email == request.resource.data.usuario;
    }
  }
}